# Rust build output
/target

# testing / runtime DB
*.sqlite
*.sqlite-*

# Local runtime config (copy teleddns-server.sample.yaml → teleddns-server.yaml)
teleddns-server.yaml

# editor swap/backup files
*.swp
*~

# Playwright MCP scratch output (browser snapshots / console logs / screenshots)
.playwright-mcp/
